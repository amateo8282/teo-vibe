<div class="min-h-[calc(100vh-86px)] flex items-center justify-center px-5">
  <div class="w-full max-w-md">
    <h1 class="text-4xl font-black text-center mb-2">로그인</h1>
    <p class="text-tv-gray text-center mb-8">TeoVibe에 오신 것을 환영합니다</p>

    <%= form_with url: session_path, class: "space-y-4" do |f| %>
      <div>
        <label for="email_address" class="block text-sm font-bold mb-1">이메일</label>
        <%= f.email_field :email_address, required: true, autofocus: true, autocomplete: "email",
          class: "w-full px-4 py-3 rounded-2xl border border-tv-light-gray/50 focus:border-tv-gold focus:outline-none text-base" %>
      </div>

      <div>
        <label for="password" class="block text-sm font-bold mb-1">비밀번호</label>
        <%= f.password_field :password, required: true, autocomplete: "current-password",
          class: "w-full px-4 py-3 rounded-2xl border border-tv-light-gray/50 focus:border-tv-gold focus:outline-none text-base" %>
      </div>

      <button type="submit" class="w-full bg-tv-black text-white rounded-pill py-3.5 text-lg font-bold hover:opacity-90 transition-opacity">
        로그인
      </button>
    <% end %>

    <div class="mt-6 text-center space-y-3">
      <%= link_to "비밀번호를 잊으셨나요?", new_password_path, class: "text-sm text-tv-gray hover:text-tv-black" %>
      <p class="text-sm text-tv-gray">
        계정이 없으신가요?
        <%= link_to "회원가입", new_registration_path, class: "text-tv-orange font-bold hover:underline" %>
      </p>
    </div>

    <div class="mt-8">
      <div class="relative">
        <div class="absolute inset-0 flex items-center"><div class="w-full border-t border-tv-light-gray/30"></div></div>
        <div class="relative flex justify-center text-sm"><span class="bg-tv-cream px-4 text-tv-gray">또는</span></div>
      </div>

      <div class="mt-6 space-y-3">
        <%= button_to "Google로 로그인", "/auth/google_oauth2", method: :post, class: "w-full flex items-center justify-center gap-3 border border-tv-light-gray/50 rounded-pill py-3 text-base font-medium hover:bg-white transition-colors" %>
        <%= button_to "카카오로 로그인", "/auth/kakao", method: :post, class: "w-full flex items-center justify-center gap-3 bg-[#FEE500] text-[#191919] rounded-pill py-3 text-base font-medium hover:opacity-90 transition-opacity" %>
      </div>
    </div>
  </div>
</div>
