이전 작업 상태를 복구하고 이어서 작업합니다.

수행 단계:

1. Git 상태 확인
   - git branch --show-current
   - git status --short
   - git log --oneline -5

2. 세션 파일 찾기 (우선순위)
   - .claude/sessions/ 폴더에서 가장 최근 파일 읽기
   - 없으면 .plans/ 폴더에서 현재 브랜치명과 일치하는 플랜 찾기
   - 둘 다 없으면 git 상태만으로 보고

   다음 정보를 사용자에게 보고:
3. 상태 보고
   - 현재 브랜치명
   - 이전 작업 내용 (세션 파일 기반)
   - 변경된 파일 목록
   - 다음 할 일 TODO
   - 관련 파일 경로

4. 확인 질문
   이어서 작업할지 사용자에게 질문