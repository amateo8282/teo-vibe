# Commit and Push Skill
1. Run `git add -A`
2. Generate a concise, descriptive commit message in Korean (한글)
3. Run `git commit` with the message
4. Push to the current branch on the correct remote
5. Show a one-line summary of what was pushed

Do NOT review or categorize changes. Just commit and push.
